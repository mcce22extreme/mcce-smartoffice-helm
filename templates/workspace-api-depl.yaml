apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.workspaceApi.appName }}-depl
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.workspaceApi.appName }}
  template:
    metadata:
      labels:
        app: {{ .Values.workspaceApi.appName }}
    spec:
      containers:
        - name: {{ .Values.workspaceApi.appName }}
          image: "{{ .Values.workspaceApi.image.name }}:{{ .Values.workspaceApi.image.tag }}"
          imagePullPolicy: Always
          env:
            - name: SMARTOFFICE_BASEADDRESS
              value: http://{{ .Values.workspaceApi.appName }}-clusterip-srv:8080
            - name: SMARTOFFICE_APPCONFIGURL
              value: {{ .Values.configUrl }}
          resources:
            requests:
              memory: {{ .Values.resources.requests.memory }}
              cpu: {{ .Values.resources.requests.cpu }}
            limits:
              memory: {{ .Values.resources.limits.memory }}
              cpu: {{ .Values.resources.limits.cpu }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.workspaceApi.appName }}-clusterip-srv
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.workspaceApi.appName }}
  ports:
    - name: {{ .Values.workspaceApi.appName }}
      protocol: TCP
      port: 80
      targetPort: 8080
